#!/bin/sh

PATH=/bin:/sbin
export PATH

printf 'MK_SECONDARY_ALIVE instance=1 pid=%s transport=mktty\n' "$$"

mount -t proc proc /proc 2>/dev/null || true
mount -t sysfs sysfs /sys 2>/dev/null || true
mount -t devtmpfs devtmpfs /dev 2>/dev/null || true

while :; do
	sleep 3600
done
